export interface Enrollment {
    id: number;
    studentId: number;
    courseId: number;
    enrollmentDate: string; // Use Date if using Angular Date handling
    // Add other fields as required
  }
  